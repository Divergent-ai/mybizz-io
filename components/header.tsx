"use client";
import Link from "next/link"; import { Menu, X } from "lucide-react"; import { useState } from "react"; import { Logo } from "./logo";
const links=[["Services","/services"],["About","/about"],["Contact","/contact"]];
export function Header(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-white/8 bg-[#090a0c]/80 backdrop-blur-xl"><div className="container flex h-18 items-center justify-between"><Logo/><nav className="hidden items-center gap-8 md:flex" aria-label="Main navigation">{links.map(([l,h])=><Link className="text-sm text-white/70 transition hover:text-white" href={h} key={h}>{l}</Link>)}<Link className="btn btn-primary text-sm" href="/contact">Book consultation</Link></nav><button className="md:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="container flex flex-col gap-5 border-t border-white/8 py-6 md:hidden">{links.map(([l,h])=><Link onClick={()=>setOpen(false)} href={h} key={h}>{l}</Link>)}<Link onClick={()=>setOpen(false)} className="btn btn-primary" href="/contact">Book consultation</Link></nav>}</header>}
